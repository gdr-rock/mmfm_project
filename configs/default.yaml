project:
  name: "JEPA-Grounded Language Planning for Procedural Videos"
  seed: 42
  device: "cuda:0"
  precision: "bf16"

runtime:
  dry_run: false
  use_dummy_data: false
  output_root: "outputs"

data:
  dataset: "crosstask"
  root: "data"
  split: "train"

planning:
  k_candidates: 8
  lambda_transition: 0.6
  mu_goal: 0.8
