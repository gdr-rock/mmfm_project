models:
  jepa:
    checkpoint: "checkpoints/vjepa2.pt"
    latent_dim: 256
    frozen: true
  planner_llm:
    name: "placeholder-llm"
    temperature: 0.7
  critic:
    hidden_dim: 256
  bridge_transition:
    latent_dim: 256
  bridge_goal:
    latent_dim: 256
