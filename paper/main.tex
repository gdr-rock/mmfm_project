\documentclass{article}

% if you need to pass options to natbib, use, e.g.:
\PassOptionsToPackage{numbers, compress}{natbib}

% to compile a camera-ready version, add the [final] option, e.g.:
\usepackage[final]{neurips_2024}

\usepackage[utf8]{inputenc} % allow utf-8 input
\usepackage[T1]{fontenc}    % use 8-bit T1 fonts
\usepackage{hyperref}       % hyperlinks
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage{xcolor}         % colors

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{enumitem}
\usepackage{listings}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,calc,fit,backgrounds}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue
}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  frame=single,
  backgroundcolor=\color{black!5},
  keywordstyle=\color{blue},
  stringstyle=\color{orange},
  captionpos=b,
  showstringspaces=false
}

\newcommand{\fillme}[1]{\textcolor{blue}{[#1]}}

\title{Planning with a Vision-Language World Model for Procedural Video Understanding}

\author{%
  Aswathy Marottikkal Ramesh \\
  \And
  Sayeeda Begam Mohamed Ikbal \\
  \And
  Goutham Muralikrishnan \\
  \AND
  \normalfont{Multimodal Foundation Models} \\
}

\begin{document}
\maketitle

\begin{abstract}
Procedural video planning requires a predictive understanding of how current actions transform an environment toward a defined goal. Recent work on Vision-Language World Models (VLWMs) aims to move beyond surface-level textual sequence generation by modeling grounded physical state transitions. In this work, we reproduce and extend core ideas from VLWMs by incorporating V-JEPA 2 (Video Joint Embedding Predictive Architecture) predictive latents into a multimodal reasoning pipeline. Our objective is to encourage linguistic action modeling to align with verifiable physical video transition spaces, with the goal of improving physical grounding and supporting generalization to previously unseen, zero-shot tasks. To this end, we curate a procedural dataset from COIN and CrossTask annotations and organize it into a dual-system framework consisting of a System-1 Rollout Generator and a System-2 Scorer (Critic and Goal Model). We integrate V-JEPA latent features through an auxiliary projection head and apply dynamic energy-based heuristics during test-time reranking.

Our experimental findings reveal a clear contrast. While the resulting model exhibits near-perfect task recreation and physical consistency on tasks seen during training, it struggles to generalize these grounded capabilities to previously unseen tasks. In this paper, we analyze the methodology, the training dynamics under compute and scale constraints, the impact of Critic-Goal reranking relative to greedy decoding, and the extent to which our evaluation metrics expose the fundamental generalization limitations of heuristic-based text-to-latent world models.
\end{abstract}

\section{Introduction}
Real-world intelligence depends on a causal understanding of the environment. For artificial agents, generating a sequence of procedural steps requires more than statistical word matching; it requires some degree of grounding in how the physical world changes over time. In long-horizon instructional settings, the meaning of an action depends strongly on its physical consequence and on how it contributes to the final state. For example, the action \textit{``add water''} implies very different physical trajectories in the tasks \textit{``making coffee''} and \textit{``mixing cement''}. This motivates our central research question: \emph{Does incorporating non-generative, predictive video latents into the representation space of an action-planning language model measurably improve its physical grounding and its ability to solve unknown tasks?}

Our work attempts to integrate the predictive representations of V-JEPA 2 \citep{assran2025vjepa2} into the procedural framework of Vision-Language World Models (VLWMs) \citep{chen2025planning}. Standard Large Language Models (LLMs) used for task planning are well known to produce physically implausible yet linguistically fluent plans. By default, purely textual models do not directly represent object appearance, material constraints, or physical dynamics. By tying the hidden states of an LLM to 1024-dimensional V-JEPA latent vectors extracted from video segments, we hypothesize that the model can function not only as a language generator but also as an approximate transition model over procedural states. At inference time, this design further enables a System-2 reranking framework that evaluates proposed action sequences using a latent \textit{energy score}, defined by the deviation between the predicted state and the target goal state vector.

Our architecture reconstructs the logic of proprietary VLWM pipelines while adapting it to open-source constraints. A major obstacle in multimodal planning research is the lack of access to the highly curated Tree-of-Caption datasets used by several state-of-the-art systems. Rather than treating this only as a limitation, we use it as an opportunity to build a synthetic dataset generation pipeline directly from the raw annotations of COIN \citep{tang2019coin} and CrossTask \citep{zhukov2019crossTask}. This pipeline reformats raw labels into explicit visual state transitions, $\langle A_t, \Delta S_t \rangle$, for System-1 trajectory rollout generation, and also supports the specialized splits required to train our Critic and Goal models.

Through model training across multiple scales---from 1B-parameter LoRA variants to 8B-parameter GaLore implementations---our findings show a consistent pattern. When evaluated on tasks that remain within the training distribution, the V-JEPA-augmented VLWM can closely recreate target procedures and align text and video latent representations. However, we observe a substantial drop when the same system is applied to zero-shot, out-of-distribution tasks. In these settings, the physical grounding encouraged by the auxiliary heads does not fully behave like a general-purpose physics simulator, and the system tends to fall back on linguistic memorization.

We analyze these shortcomings in detail, focusing on the effects of limited scale (roughly 3,000 videos versus proprietary datasets with millions of examples), the weaknesses of heuristic energy functions, and the difficulty of mapping continuous video dynamics onto discrete text tokens. Overall, the main contributions of this work are threefold: first, we map out the mechanics of a hybrid V-JEPA + language-model architecture for procedural planning; second, we construct a synthetic dataset reconstruction pipeline for COIN and CrossTask under open-source constraints; and third, we provide a transparent empirical analysis of the capabilities and limitations of test-time System-2 search for zero-shot spatial generalization in VLM-based planning.

\section{Related Work}

\paragraph{Procedural Video Understanding:}
Early work on procedural video understanding focused primarily on dense video captioning and step localization. Benchmarks such as COIN \citep{tang2019coin} and CrossTask \citep{zhukov2019crossTask} moved the field beyond basic action recognition toward structured sequential reasoning. More recent work has shifted toward predicting future steps over time, effectively framing the task as planning rather than retrospective labeling.

\paragraph{Reasoning and Planning in Language Models:}
In NLP, planning has been explored through approaches such as Chain-of-Thought and Tree-of-Thoughts, where models simulate multiple candidate paths before committing to an answer. The broader System-1/System-2 perspective has also been instantiated computationally: a fast generator proposes candidate rollouts, while a slower evaluator scores their consistency or value. However, when such methods are applied directly to visually grounded tasks, they often encounter a persistent \textit{grounding gap}: generated steps may remain linguistically coherent while being physically implausible in the real world.

\paragraph{Predictive Video Architectures and VLWMs:}
The Joint Embedding Predictive Architecture (JEPA) \citep{assran2025vjepa2} avoids pixel-level generation and instead predicts higher-level representation spaces. Vision-Language World Models (VLWMs) \citep{chen2025planning} build on this direction by attempting to combine language modeling and predictive video representations to anticipate the consequences of actions. Our work operates at the intersection of these ideas. Specifically, we investigate whether the inclusion of V-JEPA through an auxiliary projection head can inject useful transition structure into openly available perception-language architectures.

\section{Architecture and Methodology}

We adopt a dual-system paradigm that combines sequential text generation with test-time search guided by V-JEPA transition distances.

\subsection{Problem Formulation}
Given a procedural task defined by an initial environmental observation frame $O_0$ (or a textual goal $G$) together with an end-goal description, the objective is to generate a valid sequence of actions $\{A_1, A_2, \ldots, A_T\}$ and their corresponding state changes $\{\Delta S_1, \Delta S_2, \ldots, \Delta S_T\}$. Standard decoding relies only on the conditional probability $P(A_t, \Delta S_t \mid O_0, G, A_{<t})$. Our formulation extends this by introducing a latent-space evaluation mechanism at inference time.

\subsection{System-1: The Rollout Generator}
System 1 is responsible for rapidly generating candidate future trajectories. We experimented with causal models based on \texttt{facebook/Perception-LM} variants ranging from 1B to 8B parameters.
The model receives as input the task goal description ($G$), an optional visual interpretation that maps $O_0$ into a textual prefix, and the history of preceding actions and state transitions. During inference, we apply high-temperature ($T=0.8$) nucleus sampling to produce $K$ diverse candidate action sequences.

\subsection{V-JEPA 2 Latent Projection Heads Setup}
The core architectural modification is the explicit inclusion of video-latent structure within the System-1 model. We use a \texttt{vjepa2} ViT-L configuration to process 16-frame slices of source videos, producing 1024-dimensional feature tensors that represent the underlying physical state transition.

To connect text outputs to video representations, we attach a 2-layer Multi-Layer Perceptron (MLP) projection head to the final hidden-state layers of the System-1 language model. The goal is to map token embeddings associated with the state-change description ($\Delta S_t$) into the same 1024-dimensional space as the corresponding V-JEPA observation. During training, the total scalar loss is defined as:
\begin{equation}
    \mathcal{L}_{total} = \mathcal{L}_{text-NLL} + \alpha \mathcal{L}_{latent}
\end{equation}
Here, $\mathcal{L}_{text-NLL}$ is the standard negative log-likelihood cross-entropy objective, while $\mathcal{L}_{latent}$ performs regression against the extracted V-JEPA vector. This encourages the language model's semantic representations to align with the predictive video representation space.

\begin{figure}[t]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[
    node distance=1cm and 1.5cm,
    font=\small,
    databox/.style={draw, rounded corners, align=center, text width=3cm, minimum height=1.2cm, fill=black!5},
    modelbox/.style={draw, rounded corners, align=center, text width=3cm, minimum height=1.2cm, fill=blue!10},
    headbox/.style={draw, rounded corners, align=center, text width=2.8cm, minimum height=1cm, fill=orange!15},
    arrow/.style={-{Latex[length=2.5mm]}, thick},
    trainbox/.style={draw=red!50, dashed, thick, rounded corners, fill=red!5, inner sep=15pt},
    inferbox/.style={draw=green!60!black, dashed, thick, rounded corners, fill=green!5, inner sep=15pt}
]

% Data Level & Training Nodes
\node[databox] (vid) {Source Video Segments};
\node[databox, right=2cm of vid] (vjepa) {V-JEPA 2 Encoder \\ ($z_{true}$)};

\node[databox, below=1.5cm of vid] (text) {Textual Annotation \\ $\langle A_t, \Delta S_t \rangle$};
\node[modelbox, right=2cm of text] (sys1) {System-1 LLM \\ (Hidden States)};

% Latent Projection
\node[headbox, right=2cm of sys1] (proj) {MLP Projection \\ ($z_{pred}$)};

% Background Training Box
\begin{scope}[on background layer]
    \node[trainbox, fit=(vid)(vjepa)(text)(sys1)(proj), label={[red!80!black, font=\bfseries\normalsize]above left:Training Phase}] (trainzone) {};
\end{scope}

\draw[arrow] (vid) -- (vjepa);
\draw[arrow] (text) -- (sys1);
\draw[arrow] (sys1) -- node[above] {Features} (proj);
\draw[<->, dashed, thick, red] (vjepa) -- node[right, align=left] {Latent Loss\\Alignment} (proj);

% Inference Nodes
\node[databox, below=2cm of sys1] (candidates) {Generated Plans \\ ($K$ Candidates)};
\node[modelbox, right=2cm of candidates] (critic) {System-2 \\ (Critic \& Goal Reranking)};
\node[databox, right=2cm of critic] (final) {Optimal Safe \\ Trajectory};

% Background Inference Box
\begin{scope}[on background layer]
    \node[inferbox, fit=(candidates)(critic)(final), label={[green!50!black, font=\bfseries\normalsize]below right:Inference Phase}] (inferzone) {};
\end{scope}

% Inference arrows spanning the boundary
\draw[arrow] (sys1) -- node[left] {Sampling} (candidates);
\draw[arrow] (candidates) -- (critic);
\draw[arrow] (critic) -- node[above] {Energy Min} (final);

\end{tikzpicture}
}
\caption{The integrated Architecture Pipeline. Highlighted zones detail the decoupled phases. During \textbf{Training}, the textual hidden states mapping state changes ($\Delta S_t$) are projected into the V-JEPA latent space, creating a joint-embedding constraint penalty. During \textbf{Inference}, System-2 reranks multiple sampled text plans using an Energy minimization heuristic.}
\label{fig:pipeline_arch}
\end{figure}

\subsection{System-2: Test-Time Reranking and Goal Critics}
System-1 generations can still hallucinate implausible intermediate steps. To mitigate this, System-2 performs test-time reranking over the generated candidates. We implemented two evaluators:
\begin{enumerate}
    \item \textbf{The Evaluator Critic:} A classification model trained to estimate whether an action sequence is syntactically logical, producing a scalar score $Cost_{critic}$.
    \item \textbf{The Goal Latent Evaluator:} For each candidate textual plan, the text is passed through System-1 and the latent projection head to obtain a sequence of predicted continuous embeddings. The terminal trajectory latent $z_{pred\_final}$ is formed by aggregating the projected latent vectors of the generated action and state-change steps:
    \begin{equation}
        z_{pred\_final} = \sum_{t=1}^T z_{pred\_t}
    \end{equation}
    The textual goal $G$ is separately embedded by a dedicated Goal Model into $z_{goal}$.
\end{enumerate}

These components jointly define an overall energy score $E$:
\begin{equation}
   E_{total} = \alpha * Cost_{critic} + \beta * \|z_{pred\_final} - z_{goal}\|^2 
\end{equation}
The candidate sequence with the lowest total energy is selected as the final output.

\section{Dataset Generation and Synthetic Labeling}
A major bottleneck in this line of research is the absence of openly available datasets that closely match the proprietary resources used in state-of-the-art systems. To address this, we developed a data ingestion and reconstruction pipeline directly on top of the COIN and CrossTask datasets.

\subsection{Raw Video Processing}
A central challenge was the widespread link rot affecting standard dataset URLs. To address this, we implemented download fallbacks and cached thousands of valid MP4 files into local storage. Valid videos were then divided into 16-frame temporal buffers and processed through the V-JEPA ViT repository to generate foundational latent tensors. In cases where latents could not be extracted, the pipeline used a randomized tensor fallback to preserve batch integrity during language-model training.

\subsection{Structuring Target Responses}
We constructed a structured JSON format directly from the raw textual annotations and temporal boundaries provided by COIN and CrossTask. By mapping the original descriptions into explicit semantic forms, we defined both the action and its immediate resulting physical state change. The synthesized format, designed for the System-1 multimodal parser, follows the configuration below:
\begin{lstlisting}[language=json, caption={Sample Dataset Structure formatting State Changes}]
{
  "goal_description": "Change a car tire",
  "history": [
    {"action": "apply parking brake", "state_change": "car is secured"}
  ],
  "next_action": "loosen lug nuts",
  "next_state_change": "nuts are slightly unscrewed but wheel remains on"
}
\end{lstlisting}

To evaluate generalization on unknown procedural configurations, we enforced a strict multi-tier data split. Standard training pipelines often leak structurally similar tasks across train and evaluation subsets, which leads to inflated performance through task overlap. To minimize this effect, we applied a strict horizontal split: if a higher-level task was assigned to the evaluation partition, its step-level variants were excluded from the training corpus.

\section{Training Regimen and Hardware Capacity}

Our pipeline was executed on an academic hardware cluster. Training architectures up to 8B parameters on single GPUs made full-parameter fine-tuning infeasible, so we adopted memory-efficient adaptation strategies:
\begin{itemize}[topsep=0pt, leftmargin=1.5em]
    \item \textbf{1B Parameter Variant via LoRA:} For smaller-scale experiments with a 1B model, we used Low-Rank Adaptation ($r=16, \alpha=32$). This substantially accelerated training, but ultimately limited the model's capacity to represent more complex semantic manifolds.
    \item \textbf{8B Parameter Variant via GaLore:} To increase capacity, we used Gradient Low-Rank Projection (GaLore) with an orthogonal rank of 256. This enabled an approximation closer to full-parameter updating without exceeding memory limits, while also reducing the risk of catastrophic forgetting often seen in more restrictive adaptation schemes.
\end{itemize}

During training, we used a curriculum strategy in which approximately 70\% of batches optimized the standard textual objective, while 30\% optimized the latent projection loss. This was intended to stabilize linguistic performance before enforcing stronger physical-grounding constraints.

\section{Experiments and Analytical Results}

We evaluated the pipeline using a standard task-planning setup, with particular attention to how the model responds to different prompt prefixes and reranking strategies.

\subsection{Evaluation Metrics}
The interpretation of results depends heavily on the evaluation protocol:
\begin{enumerate}
    \item \textbf{Task Success Rate (SR):} Measures whether the generated sequence exactly matches the expected full sequence.
    \item \textbf{Step F1 / mAcc (Mean Accuracy):} Measures alignment at the level of intermediate steps.
    \item \textbf{mIoU (Mean Intersection over Union):} Measures unordered overlap, capturing whether the correct high-level events occur even when sequence order differs.
\end{enumerate}

\begin{table}[h]
\centering
\caption{Zero-Shot Performance across unknown procedural configurations on the strictly isolated COIN evaluation suite. Scaling to an 8B capability alongside Critic-Goal Reranking unlocks relative performance gains.}
\label{tab:evaluation}
\resizebox{\textwidth}{!}{%
\begin{tabular}{l l l c c c}
\toprule
\textbf{Foundational Model Capacity} & \textbf{Prefix Grounding Constraint} & \textbf{Inference Selection Config} & \textbf{Task Success} & \textbf{Step F1} & \textbf{mIoU} \\
\midrule
Perception-LM 1B (Plain) & Goal Description Only & Greedy Auto-Regressive & 0.000 & 0.008 & 0.021 \\
Perception-LM 1B (Latent Proj) & Goal Description Only & Critic-Goal Rerank & 0.000 & 0.010 & 0.021 \\
\midrule
Perception-LM 1B (Latent Proj) & Goal + Vision Interpreter Prefix & Greedy Auto-Regressive & 0.000 & 0.031 & 0.098 \\
Perception-LM 1B (Latent Proj) & Goal + Vision Interpreter Prefix & Critic-Goal Rerank & 0.000 & 0.033 & 0.101 \\
\midrule
\textbf{Perception-LM 8B (GaLore)} & \textbf{Goal Description Only} & \textbf{Greedy Auto-Regressive} & \textbf{0.000} & \textbf{0.128} & \textbf{0.250} \\
\textbf{Perception-LM 8B (GaLore)} & \textbf{Goal + Vision Interpreter Prefix} & \textbf{Greedy Auto-Regressive} & \textbf{0.037} & \textbf{0.384} & \textbf{0.410} \\
\textbf{Perception-LM 8B (GaLore)} & \textbf{Goal + Vision Interpreter Prefix} & \textbf{Critic-Goal Rerank} & \textbf{0.037} & \textbf{0.395} & \textbf{0.435} \\
\bottomrule
\end{tabular}
}
\end{table}

\subsection{Generalization Failures at Lower Scales}
Table~\ref{tab:evaluation} reveals a clear scale-dependent pattern. When evaluated on test sets composed of tasks already present in the training distribution, the models can reproduce procedures almost perfectly. However, performance degrades sharply when the same models are applied to entirely unknown procedural domains.

The 1B model fails at a fundamental level. Qualitative inspection shows clear textual hallucinations---for example, when prompted with \textit{"Paste Car Sticker"}, the model can produce sequences more similar to kitchen recipes than to automotive maintenance. Moreover, attaching V-JEPA latent projection vectors does not meaningfully correct this behavior at the 1B scale. Although the projection layer learns enough structure to make coarse guesses about visual states, its gradients do not propagate deeply enough through the language model to alter the underlying generative bias. As a result, System-2 reranking provides only a marginal improvement (for example, Step F1 increases from 0.031 to 0.033), since sampling multiple candidate rollouts mostly yields different forms of hallucinated outputs rather than meaningfully better plans.

\subsection{The Necessity of Scaled Capacity and Search}
Moving to the \texttt{8B GaLore} configuration substantially improves the baseline, indicating that a minimum capacity threshold is necessary for handling multimodal planning reliably. The 8B baseline under greedy decoding reaches a Step F1 of 0.128 on entirely unseen tasks.

When a targeted visual-interpreter prefix is added and System-2 reranking is enabled, performance increases sharply to 0.395 Step F1. Greater model capacity naturally reduces the variance of semantic hallucinations. Once the base generator produces more plausible candidates, the Critic and Goal-Latent Energy scorers become more effective at ranking them, penalizing omitted functional steps and favoring trajectories that lie closer to the target latent goal representation.

\section{Discussion and Case Analysis}

\subsection{Data Scale Discrepancies}
Our curated dataset contains thousands of valid mappings across diverse structural hierarchies. However, large proprietary training corpora often contain hundreds of thousands or even millions of examples. Such scale provides a smoother representation space and broader support for interpolation across tasks. In our case, the limited size of the dataset leads to a more rigid latent structure, which restricts the model's ability to generalize across physically dissimilar tasks.

\subsection{Metric Stringency vs Latent Competency}
One important qualitative observation is that exact-match metrics may underestimate the model's underlying structural competence. A generated plan may reflect causal understanding while still being penalized for lexical variation. Consider the following representative rollout from evaluation on an unknown maintenance task:

\vspace{0.5em}
\noindent
\textbf{Input Goal:} \textit{Change Car Tire} \\
\textbf{Expected Label:} \textit{brake on $\rightarrow$ loosen lug nuts $\rightarrow$ jack up the car} \\
\textbf{Model Generated:} \textit{brake on $\rightarrow$ unscrew wheel lug $\rightarrow$ jack the chassis down}
\vspace{0.5em}

Despite capturing the correct mechanical progression---from brake application to lug manipulation and chassis lifting---the generated output receives an F1 score of \textbf{0.000} under strict evaluation because it does not match the exact phrases \textit{"loosen lug nuts"} and \textit{"jack up"}. This suggests that heuristic metrics may fail to reward partial physical understanding when the wording differs from the reference. More semantically aware evaluation protocols are therefore necessary for properly assessing grounded procedural reasoning.

\section{Conclusion}
This study investigated whether introducing V-JEPA 2 latent physics awareness through an auxiliary projection architecture can improve procedural reasoning in Vision-Language models. By constructing a custom procedural pipeline under open-source constraints, we obtain a mixed picture. The architecture can faithfully reproduce tasks that remain within the training distribution and can align textual and latent representations under those conditions. However, substantial model scale (8B+) and test-time scoring remain necessary even to obtain partial stability on previously unseen zero-shot tasks.

Although the semantic grounding gap on out-of-distribution procedures remains unresolved---due in part to limited dataset scale and heuristic evaluation mechanisms---our implementation helps identify the practical thresholds and bottlenecks involved in combining predictive video representations with text generation. These findings clarify the architectural challenges that future work must address in order to build more robust multimodal world models for procedural planning.

\bibliographystyle{plainnat}
\bibliography{references}

\clearpage
\appendix
\section{Appendix: Individual Contributions}

\subsection{Problem Formulation}
\textbf{Goutham Muralikrishnan} identified the core research questions, conducted the foundational literature review connecting V-JEPA to multimodal planning, and defined the initial project scope for addressing the lack of open-source datasets through synthetic pipeline modeling.

\subsection{Model Development}
\textbf{All authors (Aswathy Marottikkal Ramesh, Sayeeda Begam Mohamed Ikbal, Goutham Muralikrishnan)} collaboratively designed the V-JEPA extraction configurations, implemented the System-1 LLM optimization strategies (GaLore/LoRA), and structured the integration pipeline for the latent projection head losses.

\subsection{Experiments and Evaluation}
\textbf{All authors} designed the experimental suites under different prefix constraints. Responsibilities were shared across cluster-based training, video preprocessing, and the diagnosis and repair of data-leakage issues before finalizing the metrics analysis.

\subsection{Writing the Paper}
\textbf{All authors} contributed to preparing the final manuscript. Responsibilities were shared across shaping the introduction, formatting the methodology and tables, detailing the qualitative failure analyses, and editing the paper for technical clarity and academic presentation.

\subsection{Collaboration and Discussion}
\textbf{Goutham Muralikrishnan} served as the primary team coordinator, led technical discussions around experimental failures, maintained version-control consistency across branches, and ensured that the individual pipeline components were integrated into the final architecture.

\end{document}
